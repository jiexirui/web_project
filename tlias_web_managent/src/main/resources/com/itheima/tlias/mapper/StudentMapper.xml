<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.tlias.mapper.StudentMapper">
    <insert id="add">
        insert into student(name, no, gender, phone, id_card,
        is_college,address,degree,graduation_date,clazz_id,create_time,update_time) values
        (#{name},#{no},#{gender},#{phone},#{idCard},#{isCollege},#{address},#{degree},#{graduationDate},#{clazzId},#{createTime},#{updateTime})
    </insert>
    <update id="update">
        update student
        <set>
            name = #{name},
            no = #{no},
            gender = #{gender},
            phone = #{phone},
            degree = #{degree},
            <if test="idCard != null and idCard != ''">
                id_card = #{idCard},
            </if>
            <if test="isCollege != null">
                is_college = #{isCollege},
            </if>
            <if test="address != null and address != ''">
                address = #{address},
            </if>
            <if test="graduationDate != null">
                graduation_date = #{graduationDate},
            </if>
            violation_count = #{violationCount},
            violation_score = #{violationScore},
            clazz_id = #{clazzId}
        </set>
        where id=#{id}
    </update>

    <delete id="deleteById">
        delete from student where id in
        <foreach item="id" collection="ids" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>


    <select id="queryStudentList" resultType="com.itheima.tlias.pojo.Student">
        select student.*, clazz.name clazz_name from student left join clazz on student.clazz_id = clazz.id
        <where>
            <if test="name != null and name != ''">
                and student.name like concat('%',#{name},'%')
            </if>
            <if test="degree != null">
                and student.degree = #{degree}
            </if>
            <if test="clazzId != null">
                and student.clazz_id = #{clazzId}
            </if>
        </where>
    </select>

    <select id="getStudentDegreeList" resultType="java.util.Map">
        select (case degree
                    when 1 then '初中'
                    when 2 then '高中'
                    when 3 then '大专'
                    when 4 then '本科'
                    when 5 then '硕士'
                    when 6 then '博士'
                    else '其他' end) name,
               count(*)              value
        from student
        group by degree;
    </select>
    <select id="getStudentCountData" resultType="java.util.Map">
        select clazz.name clazzList,
               count(*)   dataList
        from student
                 left join clazz on student.clazz_id = clazz.id
        group by clazz.name;
    </select>

</mapper>